# Exercício 4.4: Você se lembra da pesquisa binária do capítulo 4.1? Ela também é um algoritmo do tipo dividir para conquistar. Você consegue determinar um caso-base recursivo para a pesquisa binária? 
# Resposta: o caso base é quando o tamanho da lista é igual a 1. 

def pesquisa_binaria_recursiva(lista, item_pesquisado):
    if(len(lista)) == 1:
        return lista[0] if lista[0] == item_pesquisado else -1
    else:
        tamamho_lista = len(lista)/2
        if()
        return
    #aqui o algoritmo deve ser comportar da seguinte maneira
    #Pesquisa se o item é 